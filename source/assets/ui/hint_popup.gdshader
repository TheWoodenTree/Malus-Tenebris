shader_type canvas_item;

uniform float freq = 3.0;
uniform float speed = 20.0;

//void vertex() {
//	VERTEX.y += sin(3.0 * TIME + UV.x * 20.0) * 2.0;
//}

void fragment() {
	//COLOR.rgb += sin(5.0 * TIME + (SCREEN_UV.x + SCREEN_UV.y) * 20.0) * 0.2;
	//COLOR.a += sin(TIME * 10.0) / 2.0 - 0.5;
}
